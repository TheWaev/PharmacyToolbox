node_modules
dist
dist-ssr
*.local

# Local env (Supabase keys etc.) — never commit. See .env.example
.env
.env.*
!.env.example
.vite
coverage

# dm+d ingestion working files (the slim output is committed at
# src/tools/repeat-sync/data/dmd.json; the raw download/extract is not)
.dmd-cache

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*
